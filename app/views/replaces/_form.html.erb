<%= form_for(@replace) do |f| %>
  <% if @replace.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@replace.errors.count, "error") %> prohibited this replace from being saved:</h2>

      <ul>
      <% @replace.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
